/**
 * 
 */
package seg.jUCMNav.importexport.reports;

/*import com.lowagie.text.pdf.SimpleBookmark;
 import com.lowagie.text.pdf.PdfReader;
 import java.util.List;
 import java.util.Iterator;
 import java.util.Map;
 import java.util.ArrayList;*/

/**
 * @author Brad
 * 
 */
public class PDFReportBookmarks {

    /*
     * public static void RetrieveBookmarks(PdfReader pdfReader) {
     * 
     * System.out.println("=== Bookmarks ==="); List list = SimpleBookmark.getBookmark(pdfReader); for (Iterator i = list.iterator(); i.hasNext();) {
     * showBookmark((Map) i.next(), 0); }
     * 
     * 
     * }
     */

    /*
     * private static void showBookmark(Map bookmark, int indent) { for (int i = 0; i < indent; i++) { System.out.print("   "); }
     * System.out.println(bookmark.get("Title")); ArrayList kids = (ArrayList) bookmark.get("Kids"); if (kids == null) return; for (Iterator i =
     * kids.iterator(); i.hasNext();) { showBookmark((Map) i.next(), indent + 1); } }
     */

}
